[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["python311"]

[build.nixpacksPlan.phases.install]
cmds = ["pip3 install aiohttp"]

[deploy]
startCommand = "python3 scripts/live_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10